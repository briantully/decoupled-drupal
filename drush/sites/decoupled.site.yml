local:
  host: local.decoupled.com
  options: {  }
  root: /var/www/decoupled/docroot
  uri: 'http://local.decoupled.com'
  user: vagrant
  ssh:
    options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key'
